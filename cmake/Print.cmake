# -----------------------------------------------------------------------------
# print(<VARIABLE_NAME>...)
# -----------------------------------------------------------------------------
function(print)
    foreach(VAR ${ARGN})
        message("${VAR} = ${${VAR}}")
    endforeach()
endfunction()
